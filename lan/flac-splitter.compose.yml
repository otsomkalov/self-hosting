name: flac-splitter

services:
  flac-splitter:
    build: 
      context: .
      dockerfile: flac-splitter.Dockerfile
    container_name: flac-splitter
    restart: unless-stopped
    volumes:
      - /mnt/data/music:/input
      - /music:/output