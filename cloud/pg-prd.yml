version: '3.1'

services:
  pg-prd:
    container_name: pg-prd
    image: postgres:alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: ${PG_PASS}
    ports:
      - "5432:5432"
    volumes:
      - /pg/prd:/var/lib/postgresql/data